package twitter.modelview;

public class UsuarioModelViewValidator { // implements Validator {
	/*
	 * @Override public boolean supports(Class<?> classe) { return
	 * UsuarioModelView.class.isAssignableFrom(classe); }
	 * 
	 * @Override public void validate(Object objeto, Errors erro) {
	 * UsuarioModelView conta; conta = (UsuarioModelView) objeto;
	 * 
	 * Usuario usuario; usuario = conta.getUsuario();
	 * 
	 * if (!conta.getConfirmacaoSenha() .equals(usuario.getSenha())) {
	 * erro.rejectValue("confirmacaoSenha", "",
	 * "A senha e a confirmação não correspondem! Favor verificar!"); } }
	 */
}
